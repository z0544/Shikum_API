node_modules/
dist/
build/
*.log
.env
.env.local

# Legacy SQLite database (if any lingers)
server/prisma/*.db
server/prisma/*.db-journal
server/prisma/dev.db*

# Local portable PostgreSQL cluster (binaries + data)
.pg/

# Build artifacts
client/dist/
server/dist/

# OS
.DS_Store
Thumbs.db

# Source clone / scratch (analysis only, not part of the product)
_source_kms/
_xlsx_headers.txt
_distinct.txt

# Build/verification artifacts
shikum_api.tar.gz
_ui_*.png
coverage/

# Claude Code local session settings
.claude/
